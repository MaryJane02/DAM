<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:windowBackground">@drawable/light_background</item>
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorSecondary">@color/my_light_secondary</item>
        <item name="android:textColor">@color/my_light_primary</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>

    </style>

    <style name="Theme.Day" parent="Theme.MyApplication">

    </style>
    <style name="Theme.Day.Land" parent="Theme.MyApplication">
        <item name="android:windowBackground">@drawable/light_background_land</item>
    </style>
</resources>